// Main Prisma Schema File
// This file imports all the modular schema files

// Base configuration (generator and datasource)
// Must be in the main schema file for Prisma to work correctly
generator client {
  provider = "prisma-client-js"
  output   = "../../node_modules/.prisma/client"
}

datasource db {
  provider = "mongodb"
  url      = env("DB_URL")
}

// Import all schema modules
// Note: Prisma supports multiple schema files by including them in the same directory
// All .prisma files in the schema directory are automatically included

// Models are defined in:
// - schema/base.prisma (kept minimal since generator/datasource must be in main file)
// - schema/core.prisma - Core bot configuration and user management
// - schema/moderation.prisma - Moderation system
// - schema/tickets.prisma - Ticket system (comprehensive)
// - schema/entertainment.prisma - Games, polls, giveaways, events
// - schema/features.prisma - Features like roles, webhooks, aliases, economy
// - schema/system.prisma - Infrastructure (queues, automation, premium)
